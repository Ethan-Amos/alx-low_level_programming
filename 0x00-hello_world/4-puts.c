#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char code[] = "\"Programming is like building a multilingual puzzle";
		puts(code);
	return (0);
}
